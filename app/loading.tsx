import Loading from '@/components/Loader';

export default function RootLoading() {
  return (
    <Loading 
      fullScreen 
      size="large" 
      message="Loading Foresight Protocol" 
    />
  );
}